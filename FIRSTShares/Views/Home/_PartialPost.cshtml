@model Tuple<List<FIRSTShares.Entities.Categoria>, PagingList<FIRSTShares.Entities.Postagem>>
@using ReflectionIT.Mvc.Paging

@foreach (var postagem in Model.Item2.ToList())
{
    <div class="card">
        <div class="card-body">
            <h5 class="card-title"><span class="badge badge-info p-2">@postagem.Categoria.Nome</span> @postagem.Discussao.Assunto</h5>
            @{
                var pontinhos = (postagem.Conteudo.Length) < 200 ? "" : " [...]";
            }
            <p class="card-text">@Html.Raw(postagem.Conteudo.Substring(0, Math.Min(postagem.Conteudo.Length, 200)) + pontinhos)</p>
            <a href="@Url.Action("Index", "Post", new { id = postagem.ID } )" class="btn btn-info float-right" data-toggle="tooltip" data-placement="top" title="Ler mais"><i class="fab fa-readme"></i></a>
        </div>
        <div class="card-footer text-muted">
            <span class="float-left">@postagem.Usuario.NomeUsuario</span>
            <span class="float-right">@postagem.DataCriacao</span>
        </div>
    </div>
}

